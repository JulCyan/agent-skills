interface:
  display_name: "Web Performance Lab"
  short_description: "Repeatable Lighthouse evidence and optional offline HTML"
  default_prompt: "Use $web-performance-lab to measure a public page across repeatable Lighthouse runs, report protocol-safe results, and generate offline HTML only when requested."
